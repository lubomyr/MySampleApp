<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sample.app">

    <!-- ==INTERNET== --> <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".simple_activity.SimpleActivity">
        </activity>

        <activity
            android:name=".activity_with_search.ActivityWithSearch">
        </activity>

        <activity
            android:name=".activity_tab_pager.ActivityWithTabPager">
        </activity>

        <activity android:name=".fragment_with_activity_result.ActivityWithResult"></activity>

        <activity android:name=".activity_with_fragments.ActivityWithFragments"></activity>

    </application>

</manifest>